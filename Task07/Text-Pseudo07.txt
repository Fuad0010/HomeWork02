Program Task07

GET a
GET b

IF	!(a >= 10000 && a < 100000 && THEN
	b >= 10000 && b < 100000)

	SHOW "5 reqemli eded deyil"
	RETURN

ELSE

CALCULATE c = a + b

IF c < 100000 THEN

	CALCULATE c += 500000
	CALCULATE c *= 10 
	CALCULATE c += 5
	CALCULATE c *= 5 
	CALCULATE c /= 100

ELSE

	CALCULATE c += 5000000
	CALCULATE c *= 10
	CALCULATE c += 5
	CALCULATE c *= 5 
	CALCULATE c /= 100

ENDIF

SHOW c

ENDIF

ENDPROGRAM